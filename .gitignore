# Build output
/bin/
/gpm
/site/
/test/stream/echo/echo

# Local scratch/workspace dir (not part of the shipped tree)
/work/

# Local runtime data (git-backed config dir, certs, db, etc.)
/data/

# Secrets (compose env files) - never commit; .env.example is tracked
.env
deploy/.env

# Real test values - never commit; test/local.env.example is tracked
test/local.env

# Deployment doc referencing a private repo - keep local only
deploy/parallel-testing.md

# Go test / coverage artifacts
*.test
*.out
coverage.txt

# Editor / OS cruft
.idea/
.vscode/
*.swp
.DS_Store

# Local agent working notes (kept out of the public repo on purpose)
CLAUDE.md
CLAUDE.local.md
